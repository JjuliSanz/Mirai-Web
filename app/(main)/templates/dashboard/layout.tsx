export const metadata = {
  title: "Restaurant Dashboard - Manage Everything in One Place",
  description:
    "An intuitive dashboard to organize products, track progress, and streamline business management with secure access.",
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
